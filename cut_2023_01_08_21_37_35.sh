Twit Format:
Cut command line tool to quickly extract columns from a text file: cut -f 2,3 -d ':' input.txt #linux #commandline

# The cut command line tool is used to quickly extract columns from a text file
# -f defines which fields to include in the output
# -d defines the delimiter character used to separate the fields
cut -f 2,3 -d ':' input.txt
